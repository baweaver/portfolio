---
layout: default
title: Home
---
<div>
  <section class="hero-grid">
    <div class="hero-text">
      <h1 class="hero-headline">
        Engineering<br>Systems<br><em>That Endure.</em>
      </h1>
      <p class="prose">
        Software engineering for complex systems.<br>Built with clarity. Designed to last.
      </p>
      <div class="hero-ctas">
        <a href="<%= relative_url "/writing" %>" class="btn-primary">View Work →</a>
        <a href="<%= relative_url "/philosophy" %>" class="btn-secondary">About My Approach</a>
      </div>
    </div>
    <div class="hero-image">
      <div class="hero-fade"></div>
      <%== img "/images/hero_handdrawn.png", alt: "Architectural sketch", class: "hero-img" %>
    </div>
  </section>

  <section class="banner">
    <div class="banner-icons">
      <div class="text-center">
        <%== img "/images/tree-accent.png", alt: "", class: "accent-icon" %>
        <h3 class="banner-label">Clean Code</h3>
        <p class="banner-desc">Readable, maintainable code that stands the test of time.</p>
      </div>
      <div class="text-center">
        <%== img "/images/leaf-accent.png", alt: "", class: "accent-icon" %>
        <h3 class="banner-label">Thoughtful Design</h3>
        <p class="banner-desc">Intentional architecture that balances function and flexibility.</p>
      </div>
      <div class="text-center">
        <%== img "/images/box-accent.png", alt: "", class: "accent-icon" %>
        <h3 class="banner-label">System Thinking</h3>
        <p class="banner-desc">Building scalable systems that adapt to complexity, not the other way around.</p>
      </div>
      <div class="text-center">
        <%== img "/images/mountain-accent.png", alt: "", class: "accent-icon" %>
        <h3 class="banner-label">Continuous Improvement</h3>
        <p class="banner-desc">Iterating with purpose to deliver lasting value and long-term clarity.</p>
      </div>
    </div>
    <div class="banner-text-area">
      <p>
        I help organizations turn complex problems into robust, elegant software. From backend systems to developer experience, I build solutions that are reliable, scalable, and a joy to work with.
      </p>
      <a href="<%= relative_url "/contact" %>" class="banner-cta">Let's Build Something Meaningful →</a>
    </div>
  </section>

  <section class="section-padded">
    <div class="latest-header">
      <h2 class="latest-title">Latest Posts</h2>
      <span class="latest-line"></span>
      <a href="<%= relative_url "/writing" %>" class="latest-link">View All →</a>
    </div>
    <div class="latest-grid">
      <% collections.posts.resources.select { |p| p.data.date }.sort_by { |p| p.data.date }.reverse.first(3).each do |post| %>
        <a href="<%= relative_url post.relative_url %>" class="latest-card">
          <span class="latest-card-date"><%= post.data.date.strftime("%b %d, %Y") %></span>
          <h3><%= post.data.title %></h3>
          <p><%= post.data.description&.slice(0, 100) %></p>
        </a>
      <% end %>
    </div>
  </section>
</div>
